// Barrel — re-exports every component, its variants, and shared lib helpers.
// No components yet. Each new component adds one line here (see CLAUDE.md).

export { cn } from "./lib/cn";
export { Slot, Slottable } from "./lib/slot";
