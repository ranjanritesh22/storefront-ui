import { Meta } from "@storybook/blocks";

<Meta title="Introduction" />

# Storefront UI

This Storybook documents `@storefront/ui`. No components have been published yet — this is
the empty skeleton. Each new component adds its own `*.stories.tsx` alongside its source
(see CLAUDE.md for the folder shape).
